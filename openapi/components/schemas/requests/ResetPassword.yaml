type: object
properties:
  email:
    type: string
    format: email
  reset_token:
    type: string
    description: The password reset token. Must be used within 1 hour of issuance.
  new_password:
    type: string
    minLength: 8
required:
  - email
  - reset_token
  - new_password
  